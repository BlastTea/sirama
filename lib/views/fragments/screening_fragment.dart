part of 'fragments.dart';

class ScreeningFragment extends StatelessWidget {
  const ScreeningFragment({super.key});

  @override
  Widget build(BuildContext context) => const Center(child: Text('Screening Fragment'));
}
